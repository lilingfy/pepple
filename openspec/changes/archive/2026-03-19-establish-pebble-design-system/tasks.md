## 1. 基线与测试准备

- [x] 1.1 盘点现有 `apps/web` 组件、样式入口和测试基础，确认设计系统实现边界与落点文件
- [x] 1.2 为设计令牌导出编写失败测试，锁定颜色、字体、圆角、阴影和间距常量的公开名称
- [x] 1.3 为基础组件渲染建立测试目录和测试基线，确保后续组件可以独立回归验证

## 2. 设计令牌实现

- [x] 2.1 实现 Pebble 颜色、字体、圆角、阴影和动画时长令牌的最小导出，使设计令牌测试通过
- [x] 2.2 将设计令牌接入全局样式或主题入口，并验证页面可消费同一套变量
- [x] 2.3 重构令牌命名和导出结构，移除重复常量并保持测试通过

## 3. 基础组件实现

- [x] 3.1 为 `PebbleButton` 编写失败测试，覆盖默认态、悬停态、禁用态和加载态
- [x] 3.2 实现 `PebbleButton` 的最小版本并跑通测试
- [x] 3.3 为 `GlassCard` 编写失败测试，覆盖基础视觉壳和可插槽内容
- [x] 3.4 实现 `GlassCard` 的最小版本并跑通测试
- [x] 3.5 为 `PebbleInputShell` 与 `StatusPill` 编写失败测试，覆盖聚焦态和状态展示
- [x] 3.6 实现 `PebbleInputShell` 与 `StatusPill` 的最小版本并跑通测试
- [x] 3.7 重构基础组件的共享样式和类型边界，确保组件不包含业务副作用

## 4. 页面壳与布局基础

- [x] 4.1 为主导航和页面容器编写失败测试，锁定统一导航壳、激活态和内容容器结构
- [x] 4.2 实现 `MainHeader` 与 `PageShell` 的最小版本并跑通测试
- [x] 4.3 为翻译器和陪练场的三栏布局、呼吸页的居中布局编写失败测试，锁定断点退化规则
- [x] 4.4 实现共享布局组件或布局工具，使桌面端和移动端规则满足测试
- [x] 4.5 为键盘可达性和焦点可见性补充测试，确保页面壳具备基础可访问性
- [x] 4.6 重构页面壳和布局工具，消除重复布局逻辑并保持回归通过

## 5. 集成验证与文档同步

- [x] 5.1 将首页、翻译器、陪练场和呼吸页执行清单中的设计系统依赖点映射到新组件和新页面壳
- [x] 5.2 运行设计系统相关测试、类型检查和关键页面回归，记录验证结果
- [x] 5.3 同步更新相关 taskbook、执行清单和必要说明文档，确保设计系统能力边界与 OpenSpec 一致
