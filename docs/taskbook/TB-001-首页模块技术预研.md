# TB-001: 首页模块技术预研任务书

## 1. 任务书基本信息

| 项目 | 内容 |
|------|------|
| **任务书编号** | TB-001 |
| **任务书名称** | 首页模块技术预研 |
| **所属项目** | Pebble AI 情绪防御助手 |
| **预研分类** | 核心功能预研 |
| **预研深度** | 开发一代（版本预研） |
| **优先级** | P0（最高） |
| **预计工期** | 3-4 天 |
| **依赖任务** | TB-005（设计系统） |
| **状态** | 待启动 |

## 2. 需求背景

### 2.1 业务需求
首页是 Pebble AI 的门面，需要：
- 传达"情绪盔甲"核心理念
- 展示产品价值主张
- 引导用户进入核心功能
- 建立品牌信任感（统计数据）

### 2.2 用户需求
- 快速理解产品价值
- 直观感受设计风格
- 便捷导航到功能模块

## 3. 功能需求

### 3.1 页面结构

| 区块 | 功能描述 | 优先级 |
|------|---------|--------|
| 导航栏 | Logo、导航菜单、CTA按钮 | P0 |
| Hero区 | 主标题、副标题、CTA按钮组 | P0 |
| 统计区 | 四项关键数据展示 | P0 |
| 功能区 | 三项核心功能预览卡片 | P0 |
| 页脚 | 版权信息、隐私链接 | P1 |

### 3.2 详细功能

#### 3.2.1 导航栏（Navigation）

**视觉要求**:
- 固定定位，滚动时保持可见
- 背景：毛玻璃效果（backdrop-blur-xl）
- Logo：鹅卵石形状 + "Pebble AI" 文字
- 导航菜单：胶囊形状，当前页面高亮

**交互要求**:
- Logo hover：颜色变化 + 轻微旋转
- 导航项 hover：背景色变化
- 当前页面：下圆点指示器（pulse动画）

**技术要点**:
```typescript
interface NavigationProps {
  items: NavItem[];
  activePath: string;
}

interface NavItem {
  label: string;
  href: string;
  icon?: React.ReactNode;
}
```

#### 3.2.2 Hero区（HeroSection）

**视觉要求**:
- 全屏高度（min-h-screen）
- 浮动背景：3个模糊圆形，缓慢飘动
- 主标题："情绪盔甲" 带特殊效果（core-armor）
- 副标题：产品价值描述

**特殊效果 - "情绪盔甲"文字**:
- 圆角边框（pill形状）
- 多层阴影营造立体感
- 底部发光下划线
- 脉冲动画

**代码参考**:
```css
.core-armor {
  border-radius: 9999px;
  border: 1px solid rgba(143, 185, 168, 0.3);
  padding: 0 10px;
  box-shadow:
    0 2px 4px rgba(125, 140, 159, 0.2),
    0 4px 8px rgba(125, 140, 159, 0.15),
    0 8px 16px rgba(125, 140, 159, 0.1);
  letter-spacing: 1.5px;
}

.core-armor::after {
  content: '';
  position: absolute;
  width: 140%;
  height: 6px;
  background: linear-gradient(90deg, transparent, #8FB9A8, transparent);
  bottom: -15px;
  left: 50%;
  transform: translateX(-50%);
  filter: blur(4px);
  animation: pulse-glow 4s infinite;
}
```

**CTA按钮组**:
- 主按钮："立即体验" - 安全绿背景，阴影效果
- 次按钮："演示视频" - 玻璃拟态，Play图标

#### 3.2.3 统计区（StatsSection）

**视觉要求**:
- 玻璃拟态卡片容器
- 4项数据横向排列
- 每项：圆点指示 + 大数字 + 描述
- 分隔线：渐变透明

**数据项**:
| 数字 | 描述 |
|------|------|
| 98% | 言语冲突降低 |
| 1.2M | 情绪防御实例 |
| 24h | 实时心理屏障 |
| 0 | 数据隐私泄露 |

**动画效果**:
- 进入视口时数字递增动画
- hover时圆点放大

#### 3.2.4 功能区（FeatureCards）

**三项核心功能**:

| 功能 | 图标 | 标题 | 描述 |
|------|------|------|------|
| 读心翻译 | 放大镜 | AI 读心翻译器 | 剖析潜台词，提供灰岩回复 |
| 模拟陪练 | 盾牌 | 模拟陪练场 | 心理疫苗，安全练习 |
| 急救呼吸 | 风/呼吸 | 急救呼吸 | 4-7-8呼吸法找回平静 |

**卡片设计**:
- 玻璃拟态背景
- 鹅卵石形状图标容器
- hover时上浮 + 阴影增强
- 点击跳转到对应页面

## 4. 技术实现

### 4.1 组件结构

```
HomePage
├── Navigation
│   ├── Logo
│   ├── NavMenu
│   └── CTAButton
├── HeroSection
│   ├── AmbientBackground
│   ├── Badge (MVP标签)
│   ├── Headline (情绪盔甲特效)
│   ├── Subheadline
│   └── CTAButtons
├── StatsSection
│   └── GlassCard
│       ├── StatItem ×4
│       └── Divider ×3
├── FeatureSection
│   └── FeatureCard ×3
└── Footer
```

### 4.2 页面路由

```typescript
// app/page.tsx
export default function HomePage() {
  return (
    <main className="min-h-screen">
      <Navigation />
      <HeroSection />
      <StatsSection />
      <FeatureSection />
      <Footer />
    </main>
  );
}
```

### 4.3 关键组件实现

#### HeroSection 核心代码

```typescript
// components/home/HeroSection.tsx
"use client";

import { motion } from "framer-motion";

export function HeroSection() {
  return (
    <section className="relative min-h-screen flex items-center justify-center pt-20 px-6 overflow-hidden">
      {/* 浮动背景 */}
      <AmbientBackground />

      <div className="relative z-10 text-center max-w-5xl">
        {/* MVP标签 */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          className="inline-flex items-center space-x-2 px-4 py-1.5 rounded-full bg-white/50 border border-pebble-healing/30 mb-10"
        >
          <span className="w-2 h-2 rounded-full bg-pebble-healing animate-pulse" />
          <span className="text-[10px] tracking-[0.3em] text-pebble-rock uppercase">
            v1.0 MVP 已上线
          </span>
        </motion.div>

        {/* 主标题 */}
        <motion.h1
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.2 }}
          className="font-serif text-6xl md:text-[5rem] text-pebble-text mb-8 leading-tight tracking-[0.15em]"
        >
          <span className="block mb-4 text-pebble-rock">在嘈杂的世界里</span>
          <span className="inline-block">为你穿上一层</span>
          <CoreArmorText>情绪盔甲</CoreArmorText>
        </motion.h1>

        {/* 副标题 */}
        <motion.p
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.4 }}
          className="text-lg text-gray-500 mb-12 font-light tracking-widest max-w-2xl mx-auto"
        >
          基于"灰岩法"的智能心理盾牌。不教你迎合，只教你如何在恶意与压力中，保持如鹅卵石般圆润且坚定的边界。
        </motion.p>

        {/* CTA按钮 */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.6 }}
          className="flex flex-col md:flex-row items-center justify-center gap-12 mt-20"
        >
          <PrimaryCTA>立即体验</PrimaryCTA>
          <SecondaryCTA icon="play">演示视频</SecondaryCTA>
        </motion.div>
      </div>
    </section>
  );
}
```

#### CoreArmorText 特效组件

```typescript
// components/home/CoreArmorText.tsx
"use client";

import { motion } from "framer-motion";

export function CoreArmorText({ children }: { children: React.ReactNode }) {
  return (
    <span className="relative inline-block mx-4 text-[5.2rem] tracking-[0.1em] text-[#8FB9A8]">
      <span className="core-armor relative inline-block px-3">
        {children}
      </span>
      <motion.span
        className="absolute -bottom-4 left-1/2 -translate-x-1/2 w-[140%] h-1.5 bg-gradient-to-r from-transparent via-pebble-healing to-transparent rounded-full blur-sm"
        animate={{
          opacity: [0.4, 0.8, 0.4],
          scaleX: [1, 1.05, 1],
        }}
        transition={{
          duration: 4,
          repeat: Infinity,
          ease: "easeInOut"
        }}
      />
    </span>
  );
}
```

### 4.4 动画实现

#### 滚动触发动画

```typescript
// hooks/useScrollReveal.ts
"use client";

import { useInView } from "framer-motion";
import { useRef } from "react";

export function useScrollReveal(threshold = 0.15) {
  const ref = useRef(null);
  const isInView = useInView(ref, {
    once: false,
    amount: threshold
  });

  return { ref, isInView };
}
```

#### 数字递增动画

```typescript
// components/home/AnimatedNumber.tsx
"use client";

import { useEffect, useState } from "react";
import { motion, useInView } from "framer-motion";
import { useRef } from "react";

interface AnimatedNumberProps {
  value: number;
  suffix?: string;
  duration?: number;
}

export function AnimatedNumber({
  value,
  suffix = "",
  duration = 2
}: AnimatedNumberProps) {
  const [displayValue, setDisplayValue] = useState(0);
  const ref = useRef(null);
  const isInView = useInView(ref, { once: true });

  useEffect(() => {
    if (!isInView) return;

    let startTime: number;
    const animate = (currentTime: number) => {
      if (!startTime) startTime = currentTime;
      const progress = Math.min((currentTime - startTime) / (duration * 1000), 1);

      setDisplayValue(Math.floor(progress * value));

      if (progress < 1) {
        requestAnimationFrame(animate);
      }
    };

    requestAnimationFrame(animate);
  }, [isInView, value, duration]);

  return (
    <span ref={ref}>
      {displayValue}{suffix}
    </span>
  );
}
```

## 5. 验收标准

### 5.1 功能验收

| 检查项 | 验收标准 | 优先级 |
|--------|---------|--------|
| 导航栏 | 固定定位，导航正常跳转 | P0 |
| Hero区 | "情绪盔甲"特效正确显示 | P0 |
| 背景动画 | 3个圆形缓慢飘动 | P0 |
| 统计数据 | 数字递增动画 | P0 |
| 功能卡片 | hover效果，点击跳转 | P0 |

### 5.2 视觉验收

| 检查项 | 验收标准 |
|--------|---------|
| 像素还原 | 与设计稿差异 < 5px |
| 颜色一致 | 所有颜色符合设计系统 |
| 字体正确 | Noto Serif/Sans 正确加载 |
| 动画流畅 | 60fps，无卡顿 |

### 5.3 性能验收

| 指标 | 目标值 |
|------|-------|
| FCP | < 1.5s |
| LCP | < 2.5s |
| CLS | < 0.1 |
| 总资源大小 | < 500KB |

## 6. 技术风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 复杂动画性能 | 中 | 使用 transform/opacity，减少重排 |
| 字体加载延迟 | 低 | 使用 font-display: swap |
| 移动端适配 | 中 | 响应式设计，移动端优先测试 |

## 7. 参考文件

- 设计稿: `/docs/frontend/pebble_home_page.html`
- 设计系统: `/docs/taskbook/TB-005-设计系统与组件库预研.md`

## 8. 子任务与执行清单

为了方便落地，`TB-001` 进一步拆分为独立执行清单：

- [TB-001 首页模块技术预研执行清单](./TB-001-执行清单.md)

---

**文档版本**: v1.0
**创建日期**: 2026-03-16
**状态**: 待启动
